import click

def play():
    # Generate board based on size, board will always be a box
    # Randomly place stones (o) across board, placement and amount of stones will depend on difficulty
    # Clear stones from board
    # Render stone location when user enters one
    # Validate stone locations (✓)(x)
    pass

if __name__ == "__main__":
    play()
